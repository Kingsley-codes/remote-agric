"use client";
import axios from "axios"; import Image from "next/image"; import Link from "next/link"; import { useEffect, useState } from "react"; import { ArrowRight, BookOpen, Loader2 } from "lucide-react"; import { LearnPost } from "@/lib/agriLearn";
export default function AgriLearnPage() { const [posts,setPosts]=useState<LearnPost[]>([]),[loading,setLoading]=useState(true); useEffect(()=>{axios.get(`${process.env.NEXT_PUBLIC_BACKEND_URL}/api/agri-learn`).then(r=>setPosts(r.data.data.posts)).finally(()=>setLoading(false))},[]); return <main className="min-h-screen bg-[#f6f8f6]"><section className="bg-primary px-5 py-16 text-white"><div className="mx-auto max-w-7xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-green-200">Knowledge that grows</p><h1 className="mt-3 text-4xl font-black md:text-6xl">Agri-Learn</h1><p className="mt-4 max-w-2xl text-green-50">Practical farming knowledge, investment insights and stories shaping African agriculture.</p></div></section><section className="mx-auto max-w-7xl px-5 py-12">{loading?<Loader2 className="mx-auto animate-spin text-primary"/>:posts.length===0?<div className="rounded-2xl bg-white p-16 text-center"><BookOpen className="mx-auto text-gray-300"/><p className="mt-3 font-bold">New articles are coming soon.</p></div>:<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{posts.map(p=><article key={p._id} className="overflow-hidden rounded-2xl bg-white shadow-sm">{p.media?.[0]?.type==="image"?<Image src={p.media[0].url} alt={p.title} width={720} height={420} unoptimized className="h-52 w-full object-cover"/>:p.media?.[0]?.type==="video"?<video src={p.media[0].url} className="h-52 w-full object-cover" muted playsInline/>:<div className="h-52 bg-green-100"/>}<div className="p-6"><p className="text-[10px] font-extrabold uppercase tracking-widest text-primary">{p.category}</p><h2 className="mt-2 text-xl font-extrabold">{p.title}</h2><p className="mt-3 line-clamp-3 text-sm leading-6 text-gray-500">{p.excerpt}</p><Link href={`/agri-learn/${p.slug}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-primary">Read article <ArrowRight size={16}/></Link></div></article>)}</div>}</section></main> }
