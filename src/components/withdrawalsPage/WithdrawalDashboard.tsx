"use client";
import axios from "axios";import {useCallback,useEffect,useMemo,useState}from"react";import{Loader2,Search,WalletCards}from"lucide-react";
type W={_id:string;amount:number;status:"pending"|"success"|"failed";reference:string;createdAt:string;user?:{firstName:string;lastName:string;email:string;farmerID?:string}};
export default function WithdrawalDashboard(){const[items,setItems]=useState<W[]>([]),[status,setStatus]=useState("all"),[q,setQ]=useState(""),[loading,setLoading]=useState(true);const load=useCallback(async()=>{const p=new URLSearchParams();if(status!=="all")p.set("status",status);if(q)p.set("q",q);try{const r=await axios.get(`${process.env.NEXT_PUBLIC_BACKEND_URL}/api/admin/dashboard/withdrawals?${p}`,{withCredentials:true});setItems(r.data.data)}finally{setLoading(false)}},[status,q]);useEffect(()=>{const t=setTimeout(()=>void load(),250);return()=>clearTimeout(t)},[load]);const stats=useMemo(()=>({pending:items.filter(i=>i.status==="pending").reduce((s,i)=>s+i.amount,0),paid:items.filter(i=>i.status==="success").reduce((s,i)=>s+i.amount,0),count:items.length}),[items]);const money=(n:number)=>`₦${n.toLocaleString("en-NG")}`;return <section className="space-y-7 p-6 lg:p-10"><div><p className="text-xs font-medium uppercase tracking-[0.14em] text-primary">Finance operations</p><h1 className="mt-2 text-3xl font-semibold">Withdrawals</h1></div><div className="grid gap-4 md:grid-cols-3">{[["Displayed requests",stats.count],["Pending value",money(stats.pending)],["Paid value",money(stats.paid)]].map(([l,v])=><div key={l as string} className="rounded-2xl bg-white p-6 shadow-sm"><WalletCards className="text-primary"/><p className="mt-4 text-sm text-gray-500">{l}</p><p className="text-2xl font-semibold">{v}</p></div>)}</div><div className="overflow-hidden rounded-2xl bg-white shadow-sm"><div className="flex flex-col gap-3 p-5 md:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-gray-400" size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search user" className="w-full rounded-xl bg-gray-50 py-3 pl-10 pr-3"/></div><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl bg-gray-50 px-4"><option value="all">All statuses</option><option value="pending">Pending</option><option value="success">Successful</option><option value="failed">Failed</option></select></div><div className="hidden grid-cols-[1fr_130px_1fr_140px_110px] gap-3 border-t border-gray-100 bg-gray-50 px-5 py-3 text-[11px] font-semibold uppercase tracking-wider text-gray-500 md:grid"><span>User</span><span>User ID</span><span>Reference / requested</span><span>Amount</span><span>Status</span></div>{loading?<div className="flex justify-center p-16"><Loader2 className="animate-spin text-primary"/></div>:items.length===0?<div className="p-14 text-center text-gray-500">No withdrawals found.</div>:items.map(w=><div key={w._id} className="grid gap-3 border-t border-gray-100 p-5 md:grid-cols-[1fr_130px_1fr_140px_110px]"><div><p className="font-medium">{w.user?.firstName} {w.user?.lastName}</p><p className="text-xs text-gray-400">{w.user?.email}</p></div><p className="font-mono text-xs font-medium text-primary">{w.user?.farmerID ?? "Unavailable"}</p><div><p className="font-mono text-xs">{w.reference}</p><p className="text-xs text-gray-400">{new Date(w.createdAt).toLocaleString()}</p></div><p className="font-semibold">{money(w.amount)}</p><span className={`h-fit w-fit rounded-full px-3 py-1 text-xs font-medium capitalize ${w.status==="success"?"bg-green-50 text-green-700":w.status==="failed"?"bg-red-50 text-red-600":"bg-amber-50 text-amber-700"}`}>{w.status}</span></div>)}</div></section>}
